// Fixed owner-only password that gates the "lock app to one business
// category" feature in Settings. Intentionally not user-configurable —
// it is meant to stop staff from casually switching the shop's locked
// category, not to be a per-user credential.
export const CATEGORY_LOCK_PASSWORD = 'Margesh#6400';
